# Data la tupla: 
# tupla = (1, 2, 3)
# Obtenga el elemento de la posición 5, que sucede, se puede? Responder con un comentario.

tupla = (1, 2, 3)

print(tupla[5])

# no se puede obtener porque no existe la posicion 5, solo hay 3 elementos,